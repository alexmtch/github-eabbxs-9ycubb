sources:
  - name: Hello World
    handler:
      jsonSchema:
        noDeduplication: true
        operations:          
          - type: Query
            field: myobject
            method: GET
            responseSchema: schemas/schema.json#/definitions/MyObject
            responseSample: schemas/responseSample.json
